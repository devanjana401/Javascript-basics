// removes leading and trailing spaces from the string
// trimStart() or trimEnd() if you want to remove spaces only from the start or end

const str = "   anjana   ";
const trimmed = str.trim();
console.log(trimmed);

