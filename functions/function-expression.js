//Function expression is a way to define function inside an expression and assigns into a variable
//Anonymous or Named
//Not hoisted - it can't call the function befor it's defined
//Can we used as an argument
//Can we assigned to variables or objects

const sayHello = function(){
        console.log("Hi Anjana");
}
sayHello()