//create a new array by applying a function to each element
//transform the array  without changing the original array
//retur a new array with transformed values

const double = [1,2,3].map(num => num*2);
console.log(double);
